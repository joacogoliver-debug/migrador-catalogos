# Dependencias de la app de escritorio. Son dos.
#
# El backend usa http.server de la biblioteca estándar a propósito, y el
# frontend es JavaScript sin frameworks: así el ejecutable queda chico y el
# empaquetado no depende de imports dinámicos que PyInstaller no ve.

# Generar los Excel del entregable.
openpyxl>=3.1

# Ventana propia de la aplicación, para que sea un programa y no una pestaña del
# navegador. En Windows usa WebView2 (ya viene con el sistema) a través de
# pythonnet, que se instala solo como dependencia.
#
# Si falta, la app abre igual usando el motor web del sistema en modo aplicación
# (ventana sin barra de direcciones ni pestañas).
pywebview>=5.0

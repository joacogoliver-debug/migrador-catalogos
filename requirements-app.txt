# Dependencias de la app de escritorio. Son dos.
#
# El backend usa http.server de la biblioteca estándar a propósito, y el
# frontend es JavaScript sin frameworks: así el ejecutable queda chico y el
# empaquetado no depende de imports dinámicos que PyInstaller no ve.

# Generar los Excel del entregable.
openpyxl>=3.1

# NO hace falta. La app abre en su propia ventana usando el motor web que ya
# tiene la máquina (modo aplicación: sin barra de direcciones ni pestañas), así
# que no necesita un motor propio.
#
# pywebview queda como alternativa para quien la quiera al correr desde el
# código (`python app/launcher.py --ventana-nativa`). Empaquetado no sirve: su
# backend de Windows va por pythonnet/.NET y se cuelga sin abrir la ventana.
#
#   pip install pywebview
